{
  "name": "bricks",
  "version": "1.0.0",
  "description": "A game built with Electron and TypeScript",
  "main": "dist/main/main.js",
  "scripts": {
    "build": "node build.js && npm run copy-assets",
    "copy-assets": "mkdir -p dist/renderer && mkdir -p dist/assets/txt && cp src/renderer/index.html dist/renderer/ && cp src/renderer/styles.css dist/renderer/ && cp -r assets/* dist/assets/",
    "watch": "node build.js --watch",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron ."
  },
  "keywords": [
    "electron",
    "game",
    "typescript"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "electron": "^28.0.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.3.0"
  }
}
